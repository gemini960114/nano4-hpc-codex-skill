interface:
  display_name: "Nano4 Slurm Job Runner"
  short_description: "Prepare, submit, and monitor Nano4 Slurm jobs"
  default_prompt: "Use $nano4-slurm-job-runner to prepare or monitor my Nano4 job, and perform only the remote actions I explicitly authorize."
